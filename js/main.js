const WHATSAPP_NUMBER='';const menu=document.querySelector('.menu'),nav=document.querySelector('.nav');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav?.classList.toggle('open',!open)});const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');obs.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));if(/^\d{10,15}$/.test(WHATSAPP_NUMBER)){document.querySelectorAll('.whatsapp').forEach(a=>{a.href=`https://wa.me/${WHATSAPP_NUMBER}`;a.classList.add('ready')})}document.querySelectorAll('.form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();if(!form.checkValidity()){form.reportValidity();return}const status=form.querySelector('.form-status');if(status)status.textContent='Thank you. This sample form is ready to connect to an approved form service.'}));const search=document.querySelector('.search');const filters=document.querySelectorAll('.filter');function filterStories(){const q=(search?.value||'').toLowerCase(),cat=document.querySelector('.filter.active')?.dataset.filter||'all';document.querySelectorAll('.story').forEach(x=>x.classList.toggle('hidden',!x.textContent.toLowerCase().includes(q)||(cat!=='all'&&x.dataset.category!==cat)))}search?.addEventListener('input',filterStories);filters.forEach(b=>b.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));b.classList.add('active');filterStories()}));
